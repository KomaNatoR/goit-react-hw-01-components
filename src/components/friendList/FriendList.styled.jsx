import styled from '@emotion/styled';

export const List = styled.ul`
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 10px;

    margin: 10px 0 0 10px;
    padding: 0;
    min-width: 200px;
`;